# rb-scout-manager
Web App (PWA) for managing amateur soccer statistics. Manages attendance lists, performs real-time scouting (goals, assists, cards), generates automatic rankings (top scorer, best player, Fair Play, overall) and has a local backup system. Developed with pure HTML, CSS and JS using Local Storage.
